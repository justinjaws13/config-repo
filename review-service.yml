server:
  port: 9003

spring:
  application:
    name: review-service

  datasource:
    url: jdbc:postgresql://localhost:5432/reviewdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
